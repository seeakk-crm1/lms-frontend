# REST client base — axios uses paths like /auth/login → https://host/api/auth/login
VITE_API_URL=https://your-backend.onrender.com/api
# Socket.IO origin only (no /api). Optional if omitted — derived from VITE_API_URL
VITE_BACKEND_URL=https://your-backend.onrender.com
# Optional: cap reconnect attempts (default 25)
# VITE_SOCKET_MAX_RECONNECT_ATTEMPTS=25
VITE_GOOGLE_CLIENT_ID=your-google-client-id
